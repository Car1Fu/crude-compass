# Crude Compass Codebase Handoff

## 1. 文档目的

这份文档的目标不是记录历史改动，而是帮助后续 AI 或工程师快速理解当前代码库的真实结构、运行方式、模块边界、风险点，以及下一阶段接入大语言模型和 MySQL 时应该从哪里下手。

当前代码库已经不是“纯界面优化阶段”，后续重点应转向：

- 将前端中的演示数据替换为真实数据接口
- 将套保助手、预测解释、新闻解读等能力接入大语言模型
- 把页面中的“静态生成逻辑”逐步改造为“前端 UI + 后端服务 + 数据库”的结构

---

## 2. 项目整体形态

这是一个**静态前端主站**，不是标准的前后端分离工程，也不是现代打包项目。

### 2.1 当前运行方式

- 主站入口是 `index.html`
- 全站大多数功能直接写在 HTML 内嵌脚本和少量外链 JS 中
- 页面切换不是路由系统，而是前端单页切换：通过 `switchView(viewId)` 控制不同 `.view` 容器显隐
- 图表主要依赖 CDN 版 `ECharts`
- 登录/注册是演示型逻辑，不是真实鉴权
- 大量数据目前仍是前端静态数组、伪随机生成、或本地 UI 演示逻辑

### 2.2 当前文件角色概览

核心文件：

- `index.html`
- `css/styles.css`
- `js/main.js`
- `js/charts-config.js`
- `0316.html`
- `login.html`
- `register.html`
- `supply_ship_embed.js`
- `assets/`

可以把它理解为：

- `index.html`：主站壳 + 多个业务模块容器 + 大量内联业务脚本
- `js/main.js`：全站壳层脚本、视图切换、主页图表交互、登录态头像展示
- `js/charts-config.js`：新闻、研究、预测、价格看板等重业务逻辑的外链脚本
- `0316.html`：数据库页/数据筛选与展示页，独立页面，通过 iframe 嵌入主站
- `supply_ship_embed.js`：供需洞察的打包后嵌入文件，通过 iframe + Blob 注入
- `login.html` / `register.html`：独立的演示登录注册页
- `css/styles.css`：主站绝大部分样式

---

## 3. 目录与模块入口

### 3.1 主站入口

主站入口文件：

- `index.html`

头部导航点击时，会调用：

- `switchView('view-home')`
- `switchView('view-module-news')`
- `switchView('view-research')`
- `switchView('view-supply')`
- `switchView('view-database')`
- `switchView('view-api')`
- `switchView('view-forecast')`
- `switchView('view-hedge')`

对应的主视图：

- `view-home`：主页
- `view-module-news`：新闻资讯
- `view-research`：研究解读
- `view-supply`：供需洞察
- `view-database`：价格看板
- `view-api`：数据库页（实际嵌入 `0316.html`）
- `view-forecast`：预测中心
- `view-hedge`：套保助手

### 3.2 真实初始化逻辑

页面切换的真实初始化点在 `switchView(viewId)` 中：

- 进入 `view-forecast` 时触发 `initForecastOnce()`
- 进入 `view-database` 时触发 `initDatabaseView()`
- 进入 `view-api` 时触发 `_loadDbDownload()`
- 进入 `view-supply` 时触发 `_loadSupplyView()`
- 进入 `view-research` 时首次触发 `rpRenderAllGlobal()`

这几个函数分别决定了各模块的实际加载方式。

---

## 4. 一个必须先知道的事实：代码存在“重复定义 + 后覆盖”

这是当前仓库最重要的结构性风险。

### 4.1 重复最严重的文件

- `index.html`
- `js/main.js`
- `js/charts-config.js`

### 4.2 典型重复点

以下逻辑同时存在于多处：

- `switchView`
- `initForecastOnce`
- `initDatabaseView`
- `_loadDbDownload`
- 研究解读相关渲染逻辑
- 预测中心相关逻辑
- 价格看板相关逻辑

### 4.3 实际影响

后续 AI 修改时，不能假设“某函数只有一份定义”。当前代码中经常出现：

1. `index.html` 内先定义一份
2. `js/main.js` 或 `js/charts-config.js` 再定义一份
3. 文件尾部再包一层 wrapper 覆盖原函数

因此**页面真实生效的逻辑，取决于脚本加载顺序和后续覆盖**。

### 4.4 后续修改建议

如果后续继续在这个仓库上迭代，优先原则是：

- 先搜函数名，确认有几份定义
- 明确哪一份是“最后生效的”
- 不要只改一份就默认页面会变

如果后续要做大版本重构，建议第一阶段就完成：

- 把 `index.html` 中的内联业务 JS 逐步迁移到独立模块文件
- 让每个业务模块只保留一份真实实现

---

## 5. 当前核心文件说明

### 5.1 `index.html`

这是项目最重要、也最复杂的文件。

职责包括：

- 页面骨架
- 顶部导航
- 主站多个 view 容器
- 大量内联样式和业务脚本
- 若干模块初始化逻辑
- 一部分主题切换逻辑
- 一部分图表和交互逻辑

可以把它理解成：

> 一个超大号的 SPA 容器文件

里面既有 UI 结构，也有模块数据，也有初始化逻辑。

### 5.2 `js/main.js`

主要负责：

- 全站 `switchView`
- 主页顶部登录态头像展示
- 登录演示态读取
- 首页价格曲线 hover 交互

注意：

- 这里的 `switchView` 与 `index.html` 中版本存在重叠
- 登录态只是 demo，不是真实 auth

### 5.3 `js/charts-config.js`

这是当前最重的业务脚本。

主要包含：

- 新闻资讯模块逻辑
- 研究解读模块逻辑
- 预测中心逻辑
- 价格看板图表逻辑
- 数据库页 iframe 加载逻辑

这个文件的真实地位接近：

> “项目的业务逻辑主仓库”

### 5.4 `css/styles.css`

承担几乎所有主站样式：

- 首页
- 新闻资讯
- 研究解读
- 预测中心
- 价格看板
- 套保助手
- 白昼/黑夜模式样式

其中有不少模块采用命名空间方式：

- `#view-module-news.mn-light ...`
- `#view-research.rp-light ...`
- `#view-forecast.fc-light ...`
- `#view-hedge.hg-light ...`

### 5.5 `0316.html`

这是一个**独立页面**，不是 `index.html` 内部某段 HTML。

它通过 iframe 被主站数据库页加载：

- 主站 `view-api`
- iframe：`#db-download-frame`
- 加载逻辑：`window._loadDbDownload()`
- 实际载入：`0316.html`

当前它更像：

> 独立的数据指标选择、展示、图表导出页面

### 5.6 `supply_ship_embed.js`

供需洞察不是直接在主站 DOM 内写出来的，而是：

- `index.html` 中放一个 `#supply-iframe`
- `switchView('view-supply')` 时调用 `_loadSupplyView()`
- `_loadSupplyView()` 在 `supply_ship_embed.js` 中
- 该文件内部把一整份打包后的 HTML 字符串写进 Blob
- 然后通过 `iframe.src = URL.createObjectURL(blob)` 注入

也就是说，当前供需洞察是：

> “被嵌入的独立前端应用打包产物”

如果后续要深改供需模块，难点会比其他模块高。

### 5.7 `login.html` / `register.html`

这两个页面是独立的静态演示页。

当前真实行为：

- 登录页：只要邮箱和密码非空，就写入 demo 登录态并跳转首页
- 注册页：通过基础前端校验后跳转登录页
- 没有真实用户系统
- 没有真实后端注册/登录接口

---

## 6. 模块拆解

## 6.1 主页 `view-home`

主要内容：

- Hero
- 风险预警模块
- 市场快照
- 最新新闻左侧精选卡
- 首页价格走势小图

特点：

- 数据大多是静态或前端构造
- 首页价格走势图是 SVG + JS hover，不是 ECharts
- 已有基础 hover tooltip 交互

适合后续接入的方向：

- 首页快照接口化
- 首页精选新闻改为真实新闻接口
- 风险预警卡片由后端推送或查询

## 6.2 新闻资讯 `view-module-news`

当前是一个完整模块。

数据来源现状：

- 前端伪数据生成
- 分类包括地缘、宏观、供给、需求、库存、航运、价差
- 每条新闻有主题、标题、来源、影响分值

当前图片是按主题映射到本地 assets：

- 地缘：`assets/geo.png`
- 宏观：`assets/macro.png`
- 供给：`assets/supply.png`
- 需求：`assets/demand.png`
- 库存：`assets/warehouse.png`
- 航运：`assets/ship.png`
- 价差：`assets/oil.png`
- 今日重点/主页左侧精选：`assets/news.png`

适合 LLM 接入的方向：

- 新闻摘要生成
- 多条新闻聚类与主题归纳
- “点击阅读”后展示模型生成的深度解读
- 根据 MySQL 中的新闻原文和标签生成摘要、风险判断、交易含义

## 6.3 研究解读 `view-research`

当前是一个“研报列表 + 收藏/下载/筛选”的前端演示系统。

当前现状：

- `RP_REPORTS` 为前端随机生成
- 时间已被限制在 `2026-01-15` 及之前
- 机构池以国内机构为主
- 支持筛选、收藏、未读、本地导出 PDF/CSV

这部分后续很适合改成：

- 从 MySQL 查询研究报告元数据
- 从后端获取报告摘要、标签、作者、机构
- 使用 LLM 生成“核心观点”“风险提示”“三句话摘要”

## 6.4 供需洞察 `view-supply`

当前形态：

- 不直接在主 DOM 中渲染
- 通过 `iframe` 加载 `supply_ship_embed.js` 注入内容
- 内部是打包产物，不便直接维护

当前风险：

- 难以定位源代码级组件边界
- 不适合直接继续做复杂逻辑耦合

如果后续供需洞察也要接 LLM 或后端，建议优先方案：

- 如果能拿到它的源码，改源码并重新构建
- 如果不能拿到源码，尽量只把它当黑盒 iframe 模块

## 6.5 价格看板 `view-database`

这是主站内部的价格看板模块，不等于 `0316.html`。

当前包含：

- 顶部多表格行情区
- K 线图
- OPEC/需求类图表
- 裂解价差趋势
- 相关资讯

图表技术：

- ECharts

当前状态：

- 配色、均线、hover 等做过较多 UI 调整
- 仍然以静态或前端构造数据为主

后续适合改造为：

- 从后端取 OHLC、均线、库存、供需等真实数据
- 前端只保留图表渲染

## 6.6 数据库页 `view-api` -> `0316.html`

这是独立页嵌入，不要与价格看板混淆。

功能特征：

- 左侧指标树/数据项
- 右侧已选数据列表
- 支持添加、移除
- 表格/图表两种视图
- ECharts 图表渲染
- 本地导出

当前数据形态：

- 本地映射、本地演示数据
- 未在当前仓库中看到真实 SQL / API 调用

如果后续 MySQL 已经接入，这一页通常会成为：

> 第一个最自然的真实数据展示入口

建议后续把这页改成：

- 左侧数据项元数据由后端返回
- 右侧选中项查询走统一接口
- 表格与图表共用同一份后端返回数据

## 6.7 预测中心 `view-forecast`

这是当前业务复杂度最高的模块之一。

它包含：

- 中短期预测
- 因子分析
- 回测验证
- 市场一致预期
- 历史事件时间轴

当前特点：

- 逻辑分散在 `index.html` 和 `js/charts-config.js`
- 有主题切换
- 有预测图、置信区间、因子重要度、事件时间轴
- 很多结果仍是前端模拟计算

当前已知重要状态：

- 默认标的：`WTI现货`
- 默认模型：`A+P(Aurora 与 Protots)`
- 因子重要度已经与情景模拟滑杆解耦
- 市场一致预期已改为 `Brent市场一致预期`
- 历史事件时间轴已使用用户最终 Excel 版本

这块是后续接 LLM 的关键阵地之一。

推荐接法：

- **第一阶段**：保留前端预测曲线，先把“预测解释文本”和“因子文字解读”交给 LLM
- **第二阶段**：将预测结果改为后端推理结果或数据库结果
- **第三阶段**：模型、标的、预测期、因子、事件全部由服务端提供

## 6.8 套保助手 `view-hedge`

这是当前最适合优先接入 LLM 的模块。

目前已有：

- 左侧业务参数输入
- 右侧聊天窗口 UI
- 可发送用户消息
- 内部消息滚动
- 快捷短语
- 点击“计算套保方案”后自动生成一条用户问题

当前没有：

- 真实 AI 回复
- 真实上下文记忆
- 真实后端调用

因此它几乎是一个现成的 LLM UI 壳。

后续接入建议：

1. 保留现有聊天 UI
2. 将发送逻辑改为调用后端 `/api/hedge/chat`
3. 把左侧参数作为 system/context input 一并发给后端
4. 后端调用大模型，返回回答
5. 可选接入会话存储到 MySQL

---

## 7. 当前真实数据现状

### 7.1 当前仓库中明确存在的“伪数据/前端生成”

在当前工作区中，可以明确确认以下模块仍大量依赖前端数据：

- 新闻资讯
- 研究解读
- 预测中心大部分计算/表格/事件展示
- 价格看板的大量行情与图表
- 数据库页 `0316.html` 的数据项内容

### 7.2 当前仓库中未明确看到的内容

在**当前这个前端工作区**中，没有明确发现：

- Node/Java/Python/PHP 等后端项目
- MySQL 连接代码
- SQL 查询代码
- 真实 REST API 调用
- `fetch('/api/...')` 之类的完整后端接入路径

这意味着：

- “MySQL 已接入”这件事很可能发生在**别的仓库**、**别的服务**，或者尚未同步到这个前端目录
- 后续 AI 如果只拿到这个工作区，不能假设已经能直接查库

### 7.3 handoff 中应坚持的判断

对后续 AI 应明确说明：

> 当前前端仓库本身没有完整展示出 MySQL 接入路径。若真实数据库已可用，需要额外提供后端仓库、接口文档或服务地址。

---

## 8. 登录态与权限现状

当前登录/注册是演示流程，不是生产鉴权。

### 8.1 登录页 `login.html`

当前逻辑：

- 表单 submit 后阻止默认提交
- 只要邮箱和密码非空，就写入：
  - `localStorage['cc_auth_user']`
  - `sessionStorage['cc_auth_demo_once'] = '1'`
- 然后跳转 `index.html`

### 8.2 首页头像显示逻辑

首页右上角头像显示依赖：

- `localStorage['cc_auth_user']`
- `sessionStorage['cc_auth_demo_once']`

并且设计成一次性演示态：

- 登录后回主页显示头像
- 刷新页面后恢复未登录状态

### 8.3 注册页 `register.html`

当前逻辑：

- 执行前端校验
- 校验通过后跳转 `login.html`
- 没有真实用户写库

### 8.4 后续建议

如果未来接真实后端：

- 不要继续使用当前 demo 逻辑作为真实 auth 基础
- 应单独接入正式登录/注册/用户会话方案

---

## 9. 编码与文本风险

当前代码里有明显的中文编码混乱/乱码现象。

表现包括：

- 注释乱码
- 部分字符串在终端中显示异常
- 某些中文内容可读性不稳定

这意味着后续编辑时要非常注意：

- 统一文件编码为 UTF-8
- 编辑器不要自动用 GBK / ANSI 保存
- 在做大规模替换前先备份

如果后续要长期维护，建议第一阶段单独完成一次：

- 编码清理
- 注释清理
- 文案资源抽离

---

## 10. 后续接入大语言模型的最佳切入点

按投入产出比排序，建议优先级如下。

### 10.1 第一优先：套保助手

原因：

- 已有聊天 UI
- 已有业务输入表单
- 已有消息发送与滚动
- 缺的只是后端调用与回复渲染

最小改造方案：

- 前端发送消息时调用后端接口
- 请求体包含：
  - 当前用户消息
  - 左侧业务参数
  - 历史消息
- 后端调用 LLM 返回回答
- 前端将回答渲染为 AI 气泡

### 10.2 第二优先：预测中心解释能力

优先不要先让 LLM 直接“算价格预测”，而是先做：

- 预测结果文字说明
- 因子解释
- 历史事件关联说明
- 不同模型结果对比说明

原因：

- 风险低
- 能快速体现大模型价值
- 不会立即推翻现有图表结构

### 10.3 第三优先：新闻与研究的智能摘要

适合接入：

- 单条新闻摘要
- 多条新闻聚类
- 研报三句话总结
- 影响路径解释
- 面向交易/采购/套保的建议视角改写

### 10.4 第四优先：数据库页智能问答

例如：

- “帮我找近 6 个月 Brent 与库存的关系”
- “把当前右侧已选数据做一个摘要解释”
- “按月对比 WTI 和中国胜利原油”

这种模式非常适合做：

- 数据选择 + LLM 解读

---

## 11. 后续接入 MySQL 的建议架构

如果 MySQL 已经存在，建议后续不要让前端直接承担数据构造逻辑，而应变为：

- 前端：负责 UI、参数收集、图表渲染
- 后端：负责查库、清洗、聚合、调用 LLM
- MySQL：负责结构化数据与对话/日志存储

推荐接口分层：

### 11.1 市场数据接口

例如：

- `/api/market/snapshot`
- `/api/market/kline?symbol=WTI&range=1Y`
- `/api/market/series?...`

### 11.2 新闻与研究接口

例如：

- `/api/news/list`
- `/api/news/:id`
- `/api/research/list`
- `/api/research/:id`

### 11.3 预测中心接口

例如：

- `/api/forecast/curve`
- `/api/forecast/factors`
- `/api/forecast/events`
- `/api/forecast/consensus`
- `/api/forecast/explain`

### 11.4 套保助手接口

例如：

- `/api/hedge/chat`
- `/api/hedge/session/:id`
- `/api/hedge/strategy/generate`

### 11.5 数据库页接口

例如：

- `/api/data/catalog`
- `/api/data/query`
- `/api/data/export`

---

## 12. 对后续 AI 最重要的开发建议

### 12.1 不要再继续把新逻辑大量塞进 `index.html`

原因：

- 已经过大
- 存在重复定义
- 调试成本高
- 覆盖关系复杂

### 12.2 新增能力优先走“独立 JS 模块 + 最少 DOM 挂载”

建议方式：

- 新增 `js/llm-service.js`
- 新增 `js/api-client.js`
- 新增 `js/hedge-chat.js`
- 新增 `js/forecast-service.js`

先以“附加模块”方式接入，不要一开始就重写全站。

### 12.3 先替换数据源，再重构页面

优先顺序建议：

1. 真实接口替换伪数据
2. 确认页面仍能正常显示
3. 再做结构重构

这样风险最低。

### 12.4 对重复函数必须先全局排查

尤其是这些名字：

- `switchView`
- `initForecastOnce`
- `initDatabaseView`
- `_loadDbDownload`
- `_loadSupplyView`
- `renderConsensus`

### 12.5 供需洞察先视为黑盒

除非拿到源码，否则不建议先把 LLM 和后端深度耦合进 `supply_ship_embed.js`。

---

## 13. 建议的下一阶段落地顺序

如果后续工作重心转向“大模型 + MySQL”，建议实际执行顺序如下：

### 阶段 1：补齐数据与服务边界

- 明确后端仓库位置
- 明确 MySQL 表结构
- 明确前端要调的 API
- 明确哪些页面先用真实数据

### 阶段 2：优先接套保助手

- 前端消息发送 -> 后端 -> LLM -> 返回
- 保存会话上下文
- 支持左侧业务输入自动入 prompt

### 阶段 3：接预测中心解释能力

- 不先推翻现有图表
- 先增加智能解读文本与事件关联说明

### 阶段 4：接新闻与研究的摘要/归纳

- 让新闻和研报从“静态展示”变成“可解释内容”

### 阶段 5：逐步替换数据库页和价格看板的伪数据

- 后端查库
- 前端图表只负责展示

---

## 14. 最后总结

这个项目当前不是一个干净拆分的现代工程，而是一个**以 `index.html` 为核心的静态前端主站**，外加：

- 一个独立 `0316.html` 数据库页
- 一个 iframe 注入的供需洞察应用
- 若干演示型登录/注册页面

最重要的工程事实有三点：

1. 代码存在明显的重复定义与后覆盖
2. 当前大量数据仍是前端静态/伪生成
3. 在当前工作区中，没有完整暴露出真实 MySQL/API 接入代码

如果下一步目标是“接入大语言模型 + 已有 MySQL”，那么最佳策略不是继续做 UI 微调，而是：

- 先厘清后端接口边界
- 优先从套保助手和预测中心解释能力接入 LLM
- 逐步把新闻、研究、价格、数据库页的数据源从前端演示逻辑迁移到服务端

这会比继续堆前端脚本更稳，也更适合后续 AI 持续接手。

