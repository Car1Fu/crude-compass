# 当前工作进度交接

更新时间：2026-03-23  
工作区：`C:\Users\Carl Fu\Desktop\crude-compass-main-main`

## 总体说明

这个项目是单页静态前端站点，绝大多数修改集中在 [index.html](/C:/Users/Carl%20Fu/Desktop/crude-compass-main-main/index.html)、[0316.html](/C:/Users/Carl%20Fu/Desktop/crude-compass-main-main/0316.html)、[css/styles.css](/C:/Users/Carl%20Fu/Desktop/crude-compass-main-main/css/styles.css)、[js/charts-config.js](/C:/Users/Carl%20Fu/Desktop/crude-compass-main-main/js/charts-config.js)。

当前工作树里实际未提交修改只有一个文件：
- [index.html](/C:/Users/Carl%20Fu/Desktop/crude-compass-main-main/index.html)

当前环境没有浏览器预览能力，之前多数验收依赖静态代码检查，视觉效果需要下一线程手动打开页面确认。

## 近期主要工作脉络

### 1. 新闻资讯

这部分此前做过多轮样式和结构调整，主要涉及：
- 白昼 / 黑夜模式
- 各新闻模块统一成“左侧主图+标题，右侧标题列表”
- 单列纵向排列
- 影响程度颜色分级
- “查看更多 / 更多”相关调整

但用户多次回滚，当前线程没有重新核对新闻资讯最终状态。  
如果下一线程继续改新闻资讯，先以用户当前本地页面为准，不要假设此前所有改动仍然存在。

### 2. 研究解读

这部分也做过多轮调整，主要包括：
- 英文字形恢复正常
- 白昼 / 黑夜模式
- 热度颜色分级
- 导出列表按钮位置调整
- 白昼模式按钮和标签色优化

同样，因为用户多次回滚，下一线程需要先重新读当前页面代码再继续。

### 3. 0316 数据库页

这部分是改动最多的之一，历史上做过：
- 搜索栏、导出按钮位置调整
- 删除图标
- 搜索联想
- 多数据并列表格
- 图表时间轴调整
- 白昼 / 黑夜模式
- 频率下拉配色
- 左侧数据栏滚动
- 表格冻结表头

但这些改动经过大量回滚，不保证当前 [0316.html](/C:/Users/Carl%20Fu/Desktop/crude-compass-main-main/0316.html) 仍处于某个稳定终态。  
下一线程如果继续动 0316，建议先重新做一次结构盘点。

### 4. 预测中心

预测中心之前已经做过很多轮定制，包括：
- 标题和说明文案裁剪
- “价格走势 与 预测区间”改成“价格预测”
- 因子分析、回测验证文案和颜色调整
- 行业冲击卡片结构与标签样式
- 历史事件轴标签样式与滚动条样式
- 大背景图 `forecast_bg.png` 的整页背景方案

背景图部分最关键的经验：
- 不能再用会干扰悬停层级的伪元素覆盖方案
- 最后成功的方向是直接把背景和蒙版放在 `#view-forecast` 自身的 `background` 层里，而不是额外叠复杂层

如果下一线程继续动预测中心背景，优先保留当前能正常工作的因子悬停交互，不要再轻易改弹窗挂载层级。

### 5. 套保助手

这是当前最新的工作重点，也是最需要下一线程接手的部分。

已做过的稳定改动：
- 右侧原来的三个结果模块已删除，只保留一个空的大框
- `重置输入` 按钮已删除
- 左侧的 `业务类型`、`消费者类型`、`套保模型选择` 已经改成原生下拉
- `现货交易到期日期` 已经改成原生 `date`
- 页面右上已经加了 `白昼模式 / 黑夜模式` 按钮

当前卡点：
- 用户反馈：点击套保助手右上角主题按钮，白昼 / 黑夜模式仍然“没有用”
- 本线程最后一次尝试没有再改旧初始化逻辑，而是在 [index.html](/C:/Users/Carl%20Fu/Desktop/crude-compass-main-main/index.html) 里额外补了一段独立脚本，专门接管 `hgThemeBtn`
- 这段补丁脚本位于 `index.html` 中靠后、紧挨套保助手原脚本之后

下一线程处理套保助手时，建议优先检查：
- 进入 `view-hedge` 后，`#view-hedge` 是否真的被加上了 `hg-light`
- 套保助手内联 CSS 的 `#view-hedge.hg-light` 样式是否被更高优先级背景覆盖
- 是否存在旧脚本绑定和新脚本绑定同时生效或互相覆盖

## 当前 index.html 的关键现状

### 套保助手右侧

当前右侧不是原来的三块结果卡片，而是：
- 一个空的大框 `.hg-empty-stage`
- 一组隐藏的绑定节点 `.hg-hidden-bind`

隐藏节点保留的目的：
- 避免旧计算脚本继续往 `hgModelType`、`hgFutCode` 等 id 写值时直接报错

### 套保助手主题切换

当前和主题切换有关的内容有两部分：

1. 内联样式中已经存在一套 `#view-hedge.hg-light ...` 的白昼模式样式  
2. 在套保助手原脚本后面，额外补了一段独立脚本，用来重新绑定 `hgThemeBtn`

如果下一线程发现按钮还是不生效，优先检查的就是这两部分，而不是从零重写整个套保助手。

## 建议的新线程起手顺序

### 如果继续修套保助手白昼 / 黑夜模式

1. 先打开 [index.html](/C:/Users/Carl%20Fu/Desktop/crude-compass-main-main/index.html)，定位 `#view-hedge` 部分
2. 检查 `#view-hedge.hg-light` 的样式是否已经存在
3. 检查 `hgThemeBtn` 是否被绑定了多个事件
4. 真正打开页面手动点一次按钮，看 `#view-hedge` 的 class 是否变化
5. 如果 class 已变化但视觉不变，问题在 CSS
6. 如果 class 不变化，问题在 JS 绑定或初始化顺序

### 如果继续修预测中心背景

1. 不要先动因子悬停弹窗挂载逻辑
2. 先确认当前背景方案是否仍使用 `forecast_bg.png`
3. 如果改蒙版，尽量改 `background` 多层而非新增独立伪元素层

## 需要特别注意

- 用户回滚非常频繁，不要依赖“上一次已经改过”的假设
- 很多功能在 [index.html](/C:/Users/Carl%20Fu/Desktop/crude-compass-main-main/index.html) 内有内联脚本副本，不能只改外链脚本
- 当前文件中存在中文乱码显示问题，编辑时要特别小心，不要误删无关片段
- 当前线程没有运行浏览器，也没有做真实页面点击验收

